# 开发日志 — 2026-06-26

> **状态**: v2.3 重构核心完成，数据拉取完成，特征工程遇阻
> **阻塞**: 指数分钟线数据源不可用，大盘特征待解决

---

## 一、完成

### 1.1 v2.3 架构重构

| 文件 | 改动 |
|------|------|
| `backtest_engine.py` | 9 策略(5趋势+2反脆弱+2行为金融)，去掉 MACD/KDJ 硬门槛，恢复 3 个物理过滤(涨停/一字板/无量防假) |
| `sample_generator.py` | 随机搜索替代 Optuna，分层采样 9×11+1，rank-based 权重标签，10 维(含空仓) |
| `merge_samples.py` | 适配新格式 |
| `project_prompt.md` | v2.3 完整设计文档 |

### 1.2 标签方案演进

| 版本 | 标签公式 | 维度 | 采样 |
|------|---------|------|------|
| v2.2 → v2.3 | Calmar softmax → rank softmax | 9 → 10(+空仓) | 随机 → 分层 9×11 |

### 1.3 数据拉取

300 只股 × 250 交易日分钟线拉取完成（约 1800 万条）。部分新上市股票数据量不足，样本生成时会自动跳过。

### 1.4 特征工程修复

- Pandas CoW 兼容：消除所有 `inplace=True`
- 增量预热：`process_stock` 多取 800 分钟历史给滚动指标计算
- 市场情绪数据：修复 `_parse_sentiment_result` 字段名映射（zzshare 返回 `p_open/p_close` 格式）

---

## 二、遇阻

### 2.1 指数分钟线数据

zzshare `stk_mins()` 不支持指数分钟线。`000001.SH` 返回的是平安银行个股数据（close≈10.99）而非上证综指（约 3000 点）。

**已尝试**：6 种代码格式全部失败。

**可能方案**：
- A: `daily()` 获取指数日线广播到分钟
- B: 用 300 只股自建"迷你指数"
- C: 放弃大盘特征(29 维)

详见 `doc/problems/index_data_issue.md`

### 2.2 f_{code} 数据已清空

300 只股票的特征子表已全部 DROP。指数数据就绪后需重跑特征工程。

---

## 三、明天任务

1. 确认 zzshare `daily(ts_code='000001.SH')` 是否支持指数日线
2. 根据结果选择方案 A/B/C
3. 重拉指数数据 → 重跑特征工程 → 跑样本生成
