<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nessaj.quant.scada.auth.mapper.StockBaseInfoMapper">
    <select id="search" resultType="com.nessaj.quant.scada.auth.dto.StockSearchVO">
        SELECT code, name, market, sub_market, sector
        FROM stock_base_info
        WHERE (code LIKE CONCAT('%', #{keyword}, '%') OR name LIKE CONCAT('%', #{keyword}, '%'))
        <if test="subMarket != null and subMarket != ''">
            AND sub_market = #{subMarket}
        </if>
        ORDER BY code
        LIMIT #{limit}
    </select>
</mapper>
